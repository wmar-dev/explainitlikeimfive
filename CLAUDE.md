# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

A Python project named "explainitlikeimfive" - currently a minimal starter project with a single main.py entry point.

## Commands

This project uses Python 3.14 (managed via `.python-version`).

**Running the application:**
```bash
python main.py
```

## Architecture

- **main.py**: Entry point with a simple main() function
- **pyproject.toml**: Project metadata and dependencies (using modern Python packaging standards)
- **.python-version**: Python version specification (3.14)
